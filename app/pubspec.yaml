name: auth_example
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  authexampleclient:
    path: ../libs/authexampleclient 
  auto_route: ^7.8.4
  flutter_bloc: ^8.1.3
  get_it: ^7.6.4
  injectable: ^2.3.1
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.6
  auto_route_generator: ^7.3.2
  injectable_generator: ^2.4.0

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  # generate: true
